import NowPage from './now/page';

/**
 * Home page that renders the same content as the /now route.
 * @returns {JSX.Element} The rendered Now page content.
 */
export default function Home() {
	return <NowPage />;
}
