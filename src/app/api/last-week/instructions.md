- create the route same as other routes in the project
- use the API_LAST_WEEK_ID environment variable to fetch data same way in other
  routes
- parse the CSV data using papaparse
- use just obsTimeLocal and temp values from the CSV
- return the parsed data as JSON response
